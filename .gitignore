# Ignore everything by default
*

# Keep the gitignore itself versioned
!.gitignore

# Keep the main app HTML
!index.html

# Keep the external dice engine folder
!dice-3d-engine/
!dice-3d-engine/**

# Keep a future plain-text readme if added
!readme.md
!README.md
